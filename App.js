/**
 * Ponto de entrada que o Expo usa para iniciar o app.
 *
 * O Expo procura um arquivo na raiz do projeto (neste caso App.js) e
 * renderiza o componente exportado. Daqui só importamos o App "de verdade"
 * que está em src/App.jsx — assim o código da tela fica organizado na pasta src/.
 */
import App from './src/App';

export default App;
